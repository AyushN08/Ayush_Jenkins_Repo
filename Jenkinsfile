pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Example: compile code or install dependencies
                sh 'echo Build step'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                // Example: run test scripts
                sh 'echo Test step'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying the application...'
                // Example: simulate deployment
                sh 'echo Deploy step'
            }
        }
    }
}
